# Copyright (c) 2026 AIFoundry
# SPDX-License-Identifier: Apache-2.0
#
# erbium_minion build of the Arduino emlearn sample.  Inherits the
# board defconfig's full Zephyr console + Shakti UART stack.
# `Serial` resolves to ZephyrSerialStub (the variant overlay omits
# `zephyr,user::serials` deliberately) which forwards bytes through
# `printk` -> uart_poll_out on the real UART.

CONFIG_HEAP_MEM_POOL_SIZE=0
