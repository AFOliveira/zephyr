# Copyright (c) 2026 AIFoundry
# SPDX-License-Identifier: Apache-2.0
#
# Common settings.  Board-specific overlays in boards/<board>.conf
# pick between U-mode (ET-SoC1, needs ring-buffer output + ecall exit)
# and M-mode (Erbium, native Zephyr-M with real UART).

CONFIG_MULTITHREADING=n
CONFIG_BOOT_BANNER=n
CONFIG_MAIN_STACK_SIZE=4096
# Pull in the upstream emlearn Zephyr module: gives our code the
# `eml_*.h` headers (eml_trees.h, eml_net.h, etc.) that the C
# generated by `emlearn.convert(...).save(...)` references.
CONFIG_EMLEARN=y
