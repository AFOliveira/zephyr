# Copyright (c) 2026 AIFoundry
# SPDX-License-Identifier: Apache-2.0
#
# ET-SoC1: Zephyr runs in RISC-V U-mode under MachineMinion.
# Output goes to the __umode_printk_ring; real UART needs a syscall
# we deliberately don't use here.

CONFIG_RISCV_RUN_IN_UMODE=y
CONFIG_CONSOLE=n
CONFIG_SERIAL=n
