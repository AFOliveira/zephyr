# Copyright (c) 2026 AIFoundry
# SPDX-License-Identifier: Apache-2.0
#
# Erbium: Zephyr-M runs natively at M-mode with a real Shakti UART
# console.  No U-mode trickery, no MachineMinion.  Output goes to
# the UART directly; "exit" is just main() returning.

# RISCV_RUN_IN_UMODE stays at its default (n) here — Erbium board
# doesn't force it.  No override needed.
