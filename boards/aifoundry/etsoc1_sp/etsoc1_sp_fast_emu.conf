# Speed up timer-based sleeps for emulator demos.
# Tells the kernel the HW clock is 100x slower than reality,
# so k_msleep(500) completes in ~50K cycles instead of ~5M.
CONFIG_SYS_CLOCK_HW_CYCLES_PER_SEC=100000
