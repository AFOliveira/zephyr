# Speed up timer-based sleeps and busy-waits for emulator demos.
# Tells the kernel the HW clock is 100x slower than the actual 2 MHz
# mtime rate, so k_busy_wait(100000) completes in ~10K emu cycles.
CONFIG_SYS_CLOCK_HW_CYCLES_PER_SEC=20000
